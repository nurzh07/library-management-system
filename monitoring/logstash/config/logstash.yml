http.host: "0.0.0.0"
xpack.monitoring.elasticsearch.hosts: ["http://elasticsearch:9200"]

# Pipeline settings
pipeline.workers: 2
pipeline.batch.size: 125
pipeline.batch.delay: 50

# JVM settings (overridden by environment LS_JAVA_OPTS)
jvm.options:
  - "-Xms256m"
  - "-Xmx256m"
